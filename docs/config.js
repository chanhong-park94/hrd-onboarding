/*
 * 기관 설정 파일 — 이 파일만 고치면 됩니다.
 * GitHub에서 이 파일을 열고 연필(Edit)을 눌러 따옴표 안의 값만 바꾼 뒤 [Commit changes]를 누르면
 * 1~2분 뒤 사이트에 반영됩니다. 따옴표와 쉼표는 지우지 마세요.
 * 주의: 이 저장소는 공개입니다. 접속 코드와 관리자 코드는 여기에 적지 말고 Apps Script 스크립트 속성에만 두세요.
 */
window.ORG_CONFIG = {
  // 화면과 인증서에 표시할 기관 이름
  ORG_NAME: "○○직업전문학교",

  // 로고 파일 이름. docs 폴더에 logo.png를 올리고 "logo.png"로 적으세요. 없으면 "" 그대로 두세요.
  ORG_LOGO: "",

  // 구글 Apps Script 웹 앱 주소(https://script.google.com/macros/s/..../exec). README의 3단계에서 복사합니다.
  // 비워 두면 서버 없이 '미리보기 모드'로 열립니다(진도가 저장되지 않음).
  API_URL: ""
};
